<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado: Habilitado -->
    <item android:color="@color/azulClaro" android:state_enabled="true" />
    <!-- Estado: Deshabilitado (color por defecto cuando no está habilitado) -->
    <item android:color="@color/grisMedio" android:state_enabled="false" />
    <!-- Color por defecto si ninguno de los anteriores coincide  -->
    <item android:color="@color/grisMedio" />
</selector>